# Implementation review and deployment corrections

Date: 2026-09-17. Scope: extras-only runtime, actual Klippy integration, and
the deployed single-FPS OpenAMS macros. No tracked Klipper source was changed.

## Findings corrected

1. Managed macro status used Klipper's cached wrapper: a real macro observed
   `[0, 0]` across a status-changing command. The adapter now supplies the live
   wrapper and the regression observes `[0, 1]`.
2. Cancellation/failure could allow later commands inside legacy helpers and
   queued template actions. Admission checks now guard each boundary and queued
   callback, and paused managed macros cannot spawn. Cleanup remains available
   through narrowly scoped native recovery and virtual-SD error handlers.
3. Complete API scripts could execute their prefix before detecting an unclosed
   block, and overlapping submissions created runs before owning the mutex.
   Complete preflight and serialized run creation now precede execution.
4. Virtual-SD's global source flag could misclassify unrelated API/child input.
   Real worker greenlets and bound source identity now determine ownership.
   Implicit EOF joins use the file's run, preserve error status, invoke configured
   cleanup, and cannot report success after a dependency failure.
5. Ordinary print lines accumulated toward a 2 MB block limit, and completed
   runs accumulated indefinitely. Limits are now per captured block and bounded
   to 64 recent runs, without evicting active/unwinding contexts.
6. Helper return replies could leak their last device subcommand's reply.
   Replies now belong to command frames; status/result snapshots are detached.
7. Jinja `with` output was discarded and child namespaces could share mutable
   locals. Both are corrected. Unsupported inline statements and output-producing
   filter/call blocks fail clearly before execution; legacy templates are unchanged.
8. The rewritten standalone unload omitted OAMS transport. It now completes
   unloading, refreshes sensor/driver observations between phases, and aborts
   subsequent toolchanges on failure. Only loading overlaps nozzle cleaning.
   The previously active 150 mm unload assist is restored; homed/heated/unpaused
   and group guards precede hardware commands. Optional sensors remain disabled.

These are code findings and reproduced test mechanisms, not claims that the
printer physically experienced each failure.

## Verification

```text
.venv/bin/python -m pytest -q
243 passed in 2.51s

git diff --check
git -C vendor/klipper diff --exit-code
git -C vendor/klipper-upstream diff --exit-code
All exited 0.
```

The suite includes real target Klippy reactor/dispatcher, actual config/macro
parsing with inert hardware, actual virtual-SD worker failure/EOF paths, and a
fresh-process smoke against the pinned official upstream checkout. It does not
establish physical hardware behavior.

Target-environment smoke (no printer config/MCU/API opened):

```text
/home/pi/klippy-env/bin/python /tmp/gco-review.H4TEdJ/tools/smoke_klipper.py \
  --klipper /home/pi/klipper \
  --extra-parent /tmp/gco-review.H4TEdJ/klippy_extra \
  --macros /tmp/gco-review.H4TEdJ/config/oams_macros.cfg
PASS: real Klippy, real OAMS config, inert devices; repeated changes, overlap, sensor abort
Python 3.9.2
```

The deliberately failed sensor logs an expected Klipper command-error traceback.
The harness verifies the abort and absence of subsequent transport commands,
then exits 0. Jinja 3.1.6 and greenlet 2.0.2 were not upgraded.

## Deployment and recovery

Before replacing files, Moonraker reported `print_stats.state=standby`, unpaused,
and `mcu 'oams_mcu1': Unable to connect`. Installed hashes matched the previous
deployment. The plugin package and macro were staged, the old copies backed up,
Klipper stopped, the staged copies moved into place, and Klipper restarted.
After restart, the service is active and Moonraker reports the same
`mcu 'oams_mcu1': Unable to connect` error (new Klipper process ID 5794).
The log shows the OAMS identify-response timeout, not a plugin/configuration
parse failure. This is not a claim that the printer reached ready state.

- Package: `/home/pi/gco-routines/gco_routines`.
- Existing extras symlink: `/home/pi/klipper/klippy/extras/gco_routines`.
- Macro: `/home/pi/printer_data/config/oams_macros.cfg`.
- Review backup: `/home/pi/gco-routines/backups/review-20260917T191937Z`, containing
  `gco_routines/` and `oams_macros.cfg` from before this review.
- Original configuration backup from initial deployment:
  `/home/pi/printer_data/config/.gco-routines-backup-20260917T185150Z`.

Rollback only while idle: stop Klipper, preserve the current copies, restore
the package and macro from the review backup, then start Klipper. The extras
symlink and include layout do not need changes for this rollback.

Local and installed SHA-256 hashes match:

```text
eed4f224ffda02e4c64f3d743d5c96c84017ee5f8245a8c239fe3af29c70f429  __init__.py
df0e6265eee7dfa72471b1c9d19c5ccc772a19db5ed9c6cf083a4522567fd7d0  driver_api.py
9202f912cd1420048bac9999297d52f4127cb8b41dbb4c86b9e0c057fd3ec095  integration.py
cc323558cc4c2608bb2fefa370f43af7378fcbdf8d17e69701b45b27edc182b5  program.py
826b804224a30fc4b21bb12533f4300548655df84de70103170fb7277819f4f4  runtime.py
01fcc14e56471b34d1d92f4d4ba392a743460a9e89fc762ba161c7d91c517fb0  templates.py
eb87fd5ec3262be6b5183061621be725939d367ef35edd0ccc7dc8e978936279  oams_macros.cfg
```

Pi Klipper remains at `c0c7ef2a5a82f1b60c207fb02274c6536bb952cb`; both tracked
working-tree and index diffs are empty. Existing untracked extras/patches were
preserved. No upstream merge was applied: the trial merge conflicts with the
target's CAN changes in `klippy/msgproto.py`.

## Remaining limits

The initial OAMS MCU connection error was reproduced with the original
configuration and later cleared when the board became available. A supervised
T0 workflow was subsequently attempted twice. Both attempts reached the OAMS
transport boundary described below and stopped safely; a successful physical
toolchange and a multi-lane sequence remain outstanding.

Managed macros with children cannot execute inside another child. In particular,
invoke the supplied `T0`–`T3` from the default routine. Commands share the real
motion planner and hardware; software cancellation does not prove physical stop.
Geometry, material tuning, driver cancellation, and physical sensor behavior
still require supervised hardware checks.

## Live availability follow-up

After the OAMS board became available, Klipper reached `ready`, CAN discovery
reported UUID `66e4a3d0cd57`, and the controller produced live Hall/FPS telemetry.
All revised macros and the `gco_routines` object were present. A no-motion live
program completed successfully: its `live_probe` child executed `RESPOND`, the
default routine completed `WAIT ON=live_probe`, and both retained empty results
with no run fault while `print_stats` remained `standby`.

That inspection revealed that the printer deliberately sets Klipper
`min_extrude_temp: 10`, making `can_extrude=true` at room temperature. The macros
now independently require at least 170 C (configurable as
`minimum_extrude_temperature`) before toolhead extrusion, cutting, or transport.
Tests cover both toolchange and standalone-unload rejection when `can_extrude`
is true at 23 C.

The first live cold rejection also exposed a case mismatch in the configured
group preflight: tool commands intentionally remain `T0`–`T3`, but live
`configfile.settings` stores section keys as `filament_group t0`–`t3`. The
lookup now lowercases only its settings key while preserving uppercase driver
values. Tests use the real normalized status shape.

The corrected macro was staged through the Pi smoke again, backed up, installed,
and loaded after Klipper returned to `ready`. A live `T1` request at 22.97 C with
an unhomed toolhead then failed with the intended homed/heated/unpaused error.
Before and after, the toolhead position was `[0, 0, 0, 0]`, `current_group` and
`current_spool` were null, `print_stats` was `standby`, and the printer was not
paused. No load, unload, extrusion, or toolhead move was admitted.

The additional pre-follow-up macro is recoverable from
`/home/pi/gco-routines/backups/live-20260917/temperature-guard/oams_macros.cfg`.
The intermediate temperature-guard revision that still had the settings-key
case mismatch is retained beside it as `oams_macros.invalid-group.cfg` for audit;
it should not be used for rollback.

Two hardware diagnostics remain outside the routine-plugin correction: RFID
reader B reports `soft reset timed out (CommandReg=0xff)`, while reader A
initializes as version 0xA1. The proprietary OAMS firmware also exposes a rapidly
increasing `tx_retries` counter despite `rx_error=0`, `tx_error=0`, active
telemetry, and a ready printer. Those observations should be resolved or
understood before a long torture test; they did not fault the no-motion probe.

## Supervised T0 transport attempts

With the existing printer, homing, and macro configuration treated as
authoritative, T0 was attempted twice at the printer's established 260 C ABS
toolchange temperature. The two attempts followed the same observable pattern:

- the encoder advanced from approximately 3 clicks to 2237/2254 clicks;
- T0's hub Hall sensor asserted;
- the manager's one-second encoder deltas fell through 2 to 0;
- `_monitor_load_speed` paused the printer and firmware returned operation code
  6, `Spool loading cancelled`;
- `OAMSM_LOAD_FILAMENT` returned without raising, but `current_group` remained
  null; and
- the revised `_TX` state check detected that mismatch, skipped final toolhead
  extrusion and all later lanes, paused, and raised the intended toolchange
  error.

The repeatable stop near the configured 2062-click PTFE length, after the hub
sensor asserted, favors the physical slide/buffer FPS path or its OAMS
firmware/manager handoff over a gco-routines scheduling failure. The OAMS-side
FPS telemetry stayed approximately zero during the attempts. After recovery,
the separate `[fps]` object was alive and centered at 0.4994, so the two signal
paths should not be treated as the same observation. The camera covers the bed
and toolhead, not the buffer assembly, and therefore could not visually confirm
slide travel. The trace does not by itself distinguish a real slide/path stop
from a monitor false-positive.

The installed manager declares a failure after two samples whenever a load is
still marked `LOADING` and the encoder changes by less than one click; it does
not exempt an asserted hub sensor or a reached path-length endpoint. The
pre-install macro also called the same blocking `OAMSM_LOAD_FILAMENT` before
toolhead extrusion, so this condition is not introduced by moving that command
into a gco-routines child.

After each attempt, the partial path was recovered with the raw OAMS unload,
errors were cleared, and pause state was cleared. The final observed state was
`standby`, unpaused, heater target 0, XYZ homed, `current_group=null`,
`current_spool=null`, and all hub sensors clear. The first recovery returned the
encoder near 3 clicks. The second overshot zero and exposed the unsigned value
`4294967293` (that is, -3 modulo 32 bits), another OAMS telemetry edge to keep in
mind when reviewing encoder-difference logic. No T1-T3 physical attempts were
made after T0's repeated cancellation.

## FPS revision and disconnected downstream signal

The installed FPS board was subsequently identified as the local `smt32`
revision 2.0 design, not revision 2.1. Its `/HES` net still routes to MCU `PA3`
and `PA2` is unconnected, so the pin correction remains valid. However, the
revision 2.0 schematic leaves pins 5 and 6 of its six-pin downstream connector
unconnected. JP1 (`Board HES Enable`) selects the onboard/external HES source
for PA3; it does not connect that HES signal to the downstream OAMS.

The active setting was changed only from `fps:PA2` to `fps:PA3`, with the prior
file backed up as
`/home/pi/printer_data/config/oams.cfg.before-fps21-pa3-20260917`. Klipper
returned to `ready` after `FIRMWARE_RESTART`. The live FPS value changed from a
false, fixed reading near 0.499 on the unconnected PA2 input to approximately
0.0039 on the real PA3 HES input.

One instrumented T0 attempt then sampled PA3 at roughly 10 Hz. Across 148 valid
samples, the FPS value remained between 0.003418803 and 0.004151404. Meanwhile,
the OAMS encoder advanced to 2238 clicks and T0's hub Hall sensor asserted before
the same zero-speed cancellation. A later manual slide movement changed the
standalone `[fps]` PA3 reading from roughly 0.003 to roughly 0.116 while the
OAMS1 firmware's separate `fps_value` remained approximately zero. That isolates
the failure to the missing downstream signal path rather than the PA3 HES input.

The `stm32-rev2.1` schematic includes the `DiffA`/`DiffB` downstream signals
expected by the OAMS firmware. Because that firmware uses its own FPS inputs for
loading, PID following, and calibration, replacing the revision 2.0 board with
revision 2.1 is the selected correction; no partial host-side control substitute
was installed.

The partial feed was unloaded after the instrumented attempt. Final state was
`standby`, unpaused, heater target 0, XYZ homed, no current group or spool, and
all hub sensors clear. No further automatic load should be attempted until the
revision 2.1 board is installed and both the standalone `[fps]` and OAMS1 FPS
readings respond to slide travel.

## Stock-upstream serial macro fallback

The OpenAMS macro file now tests for `[gco_routines]` before rendering any
`START`, `END`, or `WAIT` control. With the extra present, OAMS load still
overlaps nozzle cleaning. With unmodified upstream Klipper, the control branch
is omitted and load/clean execute serially. Continuation helpers force fresh
renders after unload, load, inlet checking, and outlet checking so stock
Klipper's whole-template rendering cannot admit extrusion based on stale state.

The integration harness was also run without loading the gco-routines manager;
`START`, `END`, and `WAIT` were absent from Klipper's registered handlers. The
serial toolchange completed in load-then-clean order, and a simulated failed
load paused without issuing final extrusion. A separate fresh-process check ran
the macro against the pinned unmodified upstream Klipper checkout. The full
suite completed with `246 passed in 2.88s`.

The macro file was backed up on the Pi as
`/home/pi/printer_data/config/oams_macros.cfg.before-serial-fallback-20260917`,
then the updated file (SHA-256
`113a12872f823691f4e3cce39f4c3df9b7db8bc435c42bbeb6c1f715d650ff5d`) was
installed. Klipper accepted the configuration and returned to `ready`. No
tracked upstream Klipper file was changed.

## Review of serial execution despite a loaded plugin

After two unsuccessful video attempts, the live API established the actual
cause: `configfile.config` contained `gco_routines`, while
`configfile.settings` did not. The failed `macro__tx` run contained only its
default routine. Thus the macro took its serial fallback and never spawned a
loading child. The no-motion raw START probe did not exercise that branch and
could not establish that T0 used concurrency.

Klipper's `ConfigValidate._build_status_settings()` builds sections only from
read option entries. The optionless plugin reads no options, so its section
never appears there. The former test fixture invented an empty settings entry
and concealed this bug. Replacing it with the real `PrinterConfig` status builder
reproduced the one-routine outcome before the fix. The macro now checks
`'gco_routines' in printer`, which works with both the managed and stock status
wrappers. Tests verify two routines, overlap, and stock serial fallback.

The earlier speculative child-start handshake was removed; the installed
`__init__.py` is restored to hash
`eed4f224ffda02e4c64f3d743d5c96c84017ee5f8245a8c239fe3af29c70f429`.
The motion-completion `M400` after CLEAN_NOZZLE remains and is tested with a
non-yielding cleaner. No upstream changes were needed. Local verification:
`248 passed in 4.09s`, plus the inert-device smoke. The corrected macro also
passed the Pi's Python 3.9.2 inert-device smoke before installation.

Deployment backup:
`/home/pi/gco-routines/backups/capability-check-20260917T212946Z`.
Installed macro SHA-256:
`e4892d28ca14afd550522261976d6ebf193a9e875a3e458debc717a1a2eff5d0`.
Klipper returned to ready after loading these files.

### Live overlap verification after the correction

The user authorized further T0 tests without recording confirmation. After
homing and heating to 260 C, T0 was submitted once with motion-report and
routine-status sampling. The corrected run contained two routines: the default
cleaning/M400/WAIT sequence and the child executing
`OAMSM_LOAD_FILAMENT GROUP=T0`.

During the child's active load, sampled `motion_report.live_velocity` showed
actual planned toolhead movement, including the back-and-forth wipe at Y=350:

| Klipper eventtime (s) | Live X (mm) | Live Y (mm) | Velocity (mm/s) | Default command |
| --- | --- | --- | --- | --- |
| 4745.095 | 164.071 | 171.288 | 100.000 | G1 X57 F36000 |
| 4746.347 | 99.696 | 278.737 | 100.000 | G1 X57 F36000 |
| 4747.601 | 32.729 | 350.000 | 300.000 | M400 |
| 4747.851 | 50.932 | 350.000 | 272.083 | M400 |
| 4748.352 | 40.896 | 350.000 | 300.000 | M400 |
| 4748.853 | 57.000 | 350.000 | 0.000 | WAIT |

The corresponding OAMS MCU encoder observations in `klippy.log` advanced from
20 clicks at Stats 4745.7 to 228 at 4746.7, 686 at 4747.7, and 992 at 4748.7.
These overlapping observations establish toolhead motion while filament was
being transported, independently of host command-start ordering. Raw sampled
status is retained in `outputs/overlap-capability-check-20260917.jsonl`.

Loading subsequently reached 2253 clicks, stopped, and was cancelled by the
existing zero-speed monitor because the downstream FPS is still disconnected.
Final extrusion was not admitted. This validates the requested concurrent
loading/motion phase, not a complete successful toolchange. The partial feed
was unloaded and errors/pause cleared. Final verified state: standby, unpaused,
heater target 0, no current group/spool, all hub sensors clear, encoder 4,
toolhead velocity 0.

## Successful hardware retest after the FPS repair

Follow-up run: 2026-09-18 approximately 01:37–01:43 UTC (September 17 local
time). The user reported the FPS issue repaired and authorized resuming tests.
The printer had rebooted, was ready/unpaused/unloaded, and retained the corrected
macro and original runtime hashes recorded above. No software, tuning, or sensor
configuration was changed for this retest.

The OAMS-side FPS now read approximately 0.17 at idle and rose during loading;
the separate Klipper FPS reading also changed through its travel. The two
readings have different scaling/polarity and were not assumed interchangeable.
T0 initially reported present, then reported absent while homing/heating; T1
reported absent throughout. T2/T3 remained present, so only those lanes were
selected. Homing and heating to the established 260 C completed first.

| Test | Observed outcome |
| --- | --- |
| Unloaded → T2 | Driver reported loaded; both routines completed, final extrusion and position restore executed |
| T2 → T3 | Cut/retraction/unload completed before the new load; both routines completed |
| T3 again | Reported already loaded; no toolchange was started |
| T3 → T2 | Cut/retraction/unload and new load completed; both routines completed |
| SAFE_UNLOAD_FILAMENT | Returned successfully with group/spool null and all hub sensors clear |

All three load requests returned Moonraker `result=ok`; captured runs had
`fault=null` and completed default/child routines. The three traces contained
16, 3, and 3 samples, respectively, with nonzero toolhead velocity while the
OAMS loading child was running, plus 12, 6, and 6 samples of positive final
extruder velocity after a group was loaded. No sample showed a paused printer.
These counts are sampled observations, not operation-duration estimates.

The console recorded successful spool loads at Unix times 1789695572.910,
1789695652.698, and 1789695718.445. Final state was standby, unpaused, group and
spool null, all hub sensors clear, toolhead velocity 0, and heater target 0.
The final encoder was 4294967285 (−11 modulo 32 bits), the previously noted
unsigned near-zero unload overshoot; it did not cause a failure in this run.

Captured evidence:

- `outputs/fps-retest-t2-20260918T0138Z.jsonl`
- `outputs/fps-retest-t2-to-t3-20260918T0140Z.jsonl`
- `outputs/fps-retest-t3-to-t2-20260918T0141Z.jsonl`
- `outputs/fps-retest-console-20260918.json`
- `outputs/fps-retest-oams-20260918.log`

The prior downstream-FPS blocker no longer occurred on these available lanes.
This was a bounded functional test, not four-lane or long-print endurance
validation. Optional toolhead filament-switch checks remain disabled as in the
existing printer configuration; no visual extrusion-quality claim is made.

## T0 follow-up: initial load passed; unload rejected as busy

The user then reported T0 available. Its inlet status was present, the printer
was homed/unpaused/unloaded, and heating back to 260 C completed. A single `T0`
request at Unix time 1789695901.846 returned successfully; the driver reported
loaded at 1789695911.876. Run `macro__tx_6` completed both routines without a
fault, with three sampled overlaps of moving toolhead and active loading child,
six positive final-extrusion samples, and current group T0. This is another
successful concurrent load after the FPS repair.

The following `T2` request at 1789695933.818 failed during foreground T0 unloading:

- Cutting and toolhead retraction were issued. The OAMS encoder decreased from
  2271 to 2198, then remained there with T0's hub sensor asserted.
- At Klipper eventtime approximately 750.426–751.176, the log recorded repeated
  T0 `oams_f1s_hes_change` values alternating between 0 and 1.
- At 1789695944.336, `OAMSM_UNLOAD_FILAMENT` reported `OAMS is busy`.
- `_OAMS_CONFIRM_UNLOADED` saw the retained T0 group and stopped the macro with
  `OpenAMS did not finish unloading. Reload was not started`. Run `macro__tx_7`
  failed with only its default routine: no T2 loading child or final extrusion
  was admitted.
- The abort's follower-disable command was also rejected: the log records
  `Spurious response from AMS with code 2 and action 3`.
- The deployed manager leaves its state as `UNLOADING` after a rejected unload.
  Its monitor subsequently reported encoder differences 75 then 0 and emitted
  the secondary low-speed pause. This warning followed the busy rejection; it
  is not evidence that firmware accepted and then stalled the requested unload.

The installed MCU identifies as `oams_2.0.25-0c708b3`, matching the local
`mainboard-firmware` commit. Inspection of that source shows:

- `oams_cmd_unload_spool()` rejects requests while the global `busy` flag is set.
- Inlet absent-to-present edges queue automatic filament positioning. That
  worker sets `busy`, and can wait indefinitely for the hub to clear even when
  a lane is already loaded. A positioning task triggered by sensor chatter is
  a plausible explanation for this busy condition, not an observed task trace.
- `hub_hes_worker()` incorrectly emits `oams_f1s_hes_change_t` too. Consequently
  the logged event name does **not** establish whether inlet or hub chattered;
  the first commentary attributing it specifically to the inlet was corrected.
- Clearing LED errors resets `busy` without establishing that all firmware
  tasks stopped. The error-stop routine also omits the automatic positioning
  task. Forcing an error-clear/unload retry is therefore not a verified safe
  recovery for this failure.

Further motion testing was stopped pending inspection of T0's inlet/hub and
spool seating. `M104 S0` returned successfully at 1789695967.537. Last observed
state: paused, heater target 0 and cooling, group T0, current spool 0, hub 0
asserted, encoder 2198, and stationary toolhead. Filament remains in the path;
this partial toolchange must be recovered before resuming. No firmware, sensor
threshold, driver, macro, or upstream Klipper source was changed in this retest.

Captured evidence:

- `outputs/fps-retest-t0-roundtrip-20260918.jsonl`
- `outputs/fps-retest-t0-console-20260918.json`
- `outputs/fps-retest-t0-unload-failure-20260918.log`

### User-authorized error-clear and unload retry

At the user's explicit request, `OAMSM_CLEAR_ERRORS` followed by
`OAMSM_UNLOAD_FILAMENT` was sent at Unix time 1789698090.295. No cutting,
heating, toolchange, or additional toolhead extrusion was requested. The
firmware accepted transport this time: encoder progress returned from 2198 to
15. Its unload monitor then saw zero progress while hub 0 remained asserted,
set the error LED, and paused again. The driver reported `Unknown error from
OAMS` at 1789698099.393. Moonraker returned `result=ok` because this driver
reports failures without raising; that response is not unload-success evidence.
The existing PAUSE macro also moved Z from 14.2 to 16.2 and retracted E by 1
despite already being paused; these were recovery-handler side effects, not an
additional requested toolchange.

At eventtime 2917.8, hub 0 subsequently cleared and the encoder settled at 14.
No additional transport command was sent between the monitor failure and this
sensor change. Another `OAMSM_CLEAR_ERRORS` refreshed the driver's cached state.
At eventtime 2943.6–2943.8, both current group and current spool were null, all
hub sensors were clear, the toolhead was stationary, and heater target remained
0 (temperature approximately 28.7 C). Pause was deliberately retained. Thus the
path now reports unloaded, but the retry did not finish with a clean firmware
unload-success response. T0's inlet also reports absent.

Evidence: `outputs/t0-unload-retry-20260918.log` and
`outputs/t0-unload-retry-console-20260918.json`.

### T0 retry after another printer restart

On the next explicit T0 request the printer had restarted: unhomed, unpaused,
heater target 0, group/spool null, T0 inlet present, hub clear, encoder 0.
The installed macro/runtime hashes still matched the recorded deployment.
Existing `G28` homing and `M109 S260` both returned successfully before one `T0`
was submitted at Unix time 1789699439.615.

The loading child and cleaning motion overlapped (five sampled nonzero-motion
observations while the child was running), but the OAMS encoder remained 0 and
T0's hub stayed clear. The load monitor measured zero progress, set LED 0's
error, and the driver returned code 6, `Spool loading cancelled`, at
1789699443.448. The macro blocked final loading extrusion and paused. This is a
pre-hub feed/progress failure, not the earlier busy rejection or temperature
guard. No sensor threshold or timeout was changed to force progress.

`M104 S0` was sent after this failure. Subsequent external console commands
reheated to 260 C, resumed, and requested T0 again; the assistant did not issue
that second attempt or override the heater setting. Further observation also
showed no loaded group, encoder 0, and hub clear. The console-followup trace is
separate from the assistant-issued attempt.

Evidence: `outputs/t0-retry-after-restart-20260918.jsonl`,
`outputs/t0-retry-after-restart-oams-20260918.log`,
`outputs/t0-retry-after-restart-console-20260918.json`, and
`outputs/t0-console-followup-20260918.jsonl`.
