# Enhanced Klipper only: load [gco_routines] before the macro sections, then
# include oams_macros.cfg followed by this file. Klipper merges the _TX section.
# Omit this file on stock Klipper; oams_macros.cfg then runs serially unchanged.
[gcode_macro _TX]
render_mode: ordered
